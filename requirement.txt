# Python: 3.11
# Python dependencies for the transition diagram notebooks and the
# 頭痛BERT_順次と選択強化.ipynb pipeline (Sequential + MultipleChoice BERT).
# Graphviz itself is also required for the diamond/parallelogram diagrams:
#   scoop install graphviz

# Transition diagram tools
graphviz==0.21
ipykernel==7.2.0
matplotlib==3.10.9
networkx==3.6.1
PyYAML==6.0.3

# Notebook execution (jupyter / nbconvert)
jupyter==1.1.1
nbconvert==7.17.1

# Data / analytics
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.8.0
tqdm==4.67.3
seaborn==0.13.2
japanize-matplotlib==1.1.3

# PyTorch (CPU build — install via:
#   pip install torch==2.6.0+cpu --index-url https://download.pytorch.org/whl/cpu )
torch==2.6.0+cpu

# Hugging Face stack pinned to a 4.x release that loads MLukeTokenizer / BertJapaneseTokenizer correctly.
# transformers 5.x has known issues converting the SentencePiece BPE vocab for sonoisa/sentence-luke-japanese-base-lite.
transformers==4.46.3
tokenizers==0.20.3
sentencepiece==0.2.1
protobuf==7.35.0
tiktoken==0.13.0

# Japanese BERT tokenizer (Mecab + Unidic Lite)
fugashi==1.5.2
unidic-lite==1.0.8
ipadic==1.0.0
