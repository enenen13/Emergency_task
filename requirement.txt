# Python: 3.11
# Python dependencies for the transition diagram notebooks and the
# 頭痛BERT_順次と選択強化.ipynb pipeline (Sequential + MultipleChoice BERT).
#
# Install:
#   pip install -r requirement.txt
#
# Graphviz の実行ファイル本体（dot.exe）も別途必要:
#   scoop install graphviz   # Windows
#
# torch は CPU ビルドを使うため PyPI とは別の index から取る必要がある。
# 下の --extra-index-url が pip にCPU版の置き場を教える。
--extra-index-url https://download.pytorch.org/whl/cpu

# ---- 遷移図ツール ----
graphviz>=0.20
ipykernel>=6.0
matplotlib>=3.7
networkx>=3.0
PyYAML>=6.0

# ---- ノートブック実行（jupyter / nbconvert） ----
jupyter>=1.0
nbconvert>=7.0

# ---- データ / 分析 ----
pandas>=2.0
numpy>=1.24,<3
scikit-learn>=1.3
tqdm>=4.65
seaborn>=0.12
japanize-matplotlib>=1.1

# ---- PyTorch (CPU build) ----
# Windowsで torch 2.12.x は DLL ロードに失敗する事例があり、2.6〜2.7 を推奨。
torch>=2.6,<2.10

# ---- Hugging Face スタック ----
# transformers 5.x は MLukeTokenizer / sonoisa/sentence-luke-japanese-base-lite の
# SentencePiece 変換で TypeError が出るため、必ず 4.x 系を使う。
transformers>=4.46,<5
tokenizers>=0.20,<0.22
sentencepiece>=0.2
protobuf>=4.0
tiktoken>=0.7

# ---- 日本語 BERT 用 Mecab + Unidic Lite ----
fugashi>=1.5
unidic-lite>=1.0
ipadic>=1.0
